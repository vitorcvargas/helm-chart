apiVersion: v1
kind: Service
metadata:
  name: {{ include "financial-info.fullname" . }}
  labels:
    {{- include "financial-info.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "financial-info.selectorLabels" . | nindent 4 }}
