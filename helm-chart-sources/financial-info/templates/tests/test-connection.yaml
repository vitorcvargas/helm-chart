apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "financial-info.fullname" . }}-test-connection"
  labels:
    {{- include "financial-info.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "financial-info.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
